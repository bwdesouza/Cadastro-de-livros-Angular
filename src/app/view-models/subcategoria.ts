export class SubCategoria {
    id: number;
    idCategoria: number;
    descricao: string;

    // constructor(id: number, descricao: string){
    //     this.id = id;
    //     this.descricao = descricao;
    // }
}